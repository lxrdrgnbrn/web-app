import { observer } from 'mobx-react-lite';
import React from 'react';

const Admin = observer( () => {
    return (
        <div>
            <h1 style={{"fontWeight": "500per"}}>Ты пидор</h1>
        </div>
    );
})

export default Admin;